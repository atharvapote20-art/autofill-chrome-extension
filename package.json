{
  "name": "secure-form-vault",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "icons": "node scripts/build-icons.mjs",
    "prebuild": "node scripts/build-icons.mjs",
    "build": "tsc --noEmit && vite build && vite build --config vite.content.config.ts",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "sharp": "^0.33.5",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  }
}
